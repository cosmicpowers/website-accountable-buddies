// We are hiding the flash message at the top of the screen,
  // sliding it down for three seconds, delaying it for 2 seconds,
 // and finally sliding it up for two seconds.

$('#flashMessage').hide().slideDown(3000).delay(2000).slideUp(2000);

// TODO onclick event one to hide and one to show jquery 
